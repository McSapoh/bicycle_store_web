<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-dark border-bottom box-shadow">
    <div class="justify-content-center col-md-2">
        <a class="navbar-brand text-white text-center" asp-area="" asp-controller="User" asp-action="Bicycles">Bicycle Store</a>
    </div>
    <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between row">
        <ul class="navbar-nav flex-grow-1 col-md-10"></ul>
        <ul class="navbar-nav flex-grow-1">
            @{
                @using Microsoft.AspNetCore.Builder;
                @using Microsoft.AspNetCore.Localization;
                @using Microsoft.Extensions.Options;

                @inject IOptions<RequestLocalizationOptions> LocOptions
                var requestCulture = Context.Features.Get<IRequestCultureFeature>();
                var cultureItems = LocOptions.Value.SupportedUICultures
                .Select(c => new SelectListItem { Value = c.Name, Text = c.DisplayName })
                .ToList();
            }
            @* <div>
                <form asp-controller="User"
                      asp-action="SetLanguage"
                      asp-route-returnUrl="@Context.Request.Path" method="post">
                    <select name="culture" onchange="this.form.submit();"
                            asp-for="@requestCulture.RequestCulture.UICulture.Name"
                            asp-items="cultureItems" class="form-select-sm">
                    </select>
                </form>
            </div> *@
            @{
                if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item dropdown" style="float:right;">
                        <a href="#" class="nav-link text-white">@User.Identity.Name</a>
                        <ul class="dropdown-content bg-dark list-unstyled">
                            <li>
                                <a class="nav-link text-white" asp-area="" asp-controller="User" asp-action="SetProfile">@SharedLocalizer["change"] @SharedLocalizer["profile"]</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-white" asp-area="" asp-controller="ShoppingCart" asp-action="Index">@SharedLocalizer["shoppingCart"]</a>
                            </li>
                            <li>
                                <a class="nav-link text-white" asp-area="" asp-controller="Order" asp-action="UserOrders">@SharedLocalizer["orders"]</a>
                            </li>
                            <li>
                                <a class="nav-link text-white" asp-area="" asp-controller="User" asp-action="Logout">@SharedLocalizer["log out"]</a>
                            </li>
                        </ul>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link text-white" asp-area="" asp-controller="User" asp-action="SetProfile">@SharedLocalizer["register"]</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" asp-area="" asp-controller="User" asp-action="Login" asp-route-ReturnUrl="@{
                        @Context.Request.Path
    }">@SharedLocalizer["log in"]</a>
                    </li>
                }
            }
        </ul>
    </div>
</nav>
